tool: gofmt
provides: [check]
# Pass iff no files are unformatted. gofmt -l lists offenders; -z asserts empty.
steps:
  check:
    command: "test -z \"$(gofmt -l .)\" || { echo 'unformatted:'; gofmt -l .; false; }"
