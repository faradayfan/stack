tool: pnpm-script
detect: "node --version"
setup:
  asdf: nodejs
  dir: frontend          # node/pnpm pin + detect live in the frontend subdir
provides: [check]
steps:
  check:
    command: "pnpm install --frozen-lockfile && pnpm run {{.script}}"
